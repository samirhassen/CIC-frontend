# CIC Frontend (Power BI embed UI)

Angular shell that:
- Accepts a Protech-issued `pa_token`, validates it, and requests the embed config.
- Renders the correct Power BI report using `powerbi-client`.

Full integration and configuration documentation lives in the backend repository: `CIC-Backend/README.md`.
